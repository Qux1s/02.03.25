#include <bitset>
#include <iostream>

std::bitset<4> rotl(std::bitset<4> bits)
{
	std::bitset<4> bitsTemp{ 0b1000 };
	if (static_cast<int>(bits.to_ulong()) >= 8)
	{
		bits <<= 1;
			bits.flip(0);
			return bits;
	}
	return bits << 1;
}

int main()
{
	std::bitset<4> bits1{ 0b0001 };
	std::cout << rotl(bits1) << '\n';

	std::bitset<4> bits2{ 0b1001 };
	std::cout << rotl(bits2) << '\n';

	return 0;
}
